# Decisions log

One line per decision, gotcha, or deviation from the spec. Newest at the bottom.

- 2026-09-20: Repo name assumed `AA-BuildingBlocks` (GitHub does not allow spaces); `base` is `/AA-BuildingBlocks/` in vite.config.js.
- 2026-09-20: Pinned deps: three 0.186.0, vite 8.3.0, gsap 3.15.0, idb-keyval 6.3.0, vite-plugin-pwa 1.3.0, @fontsource/pixelify-sans 5.3.0.
- 2026-09-20: Font is Pixelify Sans (OFL-1.1) bundled via @fontsource (latin 400/700 only); fallback stack is Nunito / ui-rounded / system-ui.
- 2026-09-20: PWA icons are generated by `tools/make-icons.mjs` (a dependency-free PNG encoder over a 32x32 pixel-art drawing) and checked in under public/.
- 2026-09-20: Stage 1 boots straight into Profile 1 (Atharv) because the profile picker is Stage 4.
- 2026-09-20: Stage 1 includes a minimal "Tap to play" card (needed to unlock iOS audio) and place/break/tap/footstep sounds only; ambient/music wait for Stage 3.
- 2026-09-20: Atlas bleed: tiles are drawn at full 64 px and UVs are inset by 2 px; GPU mipmaps are box filters aligned to tile boundaries, so cross-tile bleeding only starts at mip level 3 (8 px tiles) where it is invisible.
- 2026-09-20: Leaves are opaque cubes (not cutout) for performance and richer AO; the cutout material is used by flowers, tufts, wheat and torches.
- 2026-09-20: No light propagation / sunlight columns: the world is open-air, so per-vertex AO + shadows carry the look. Torch point lights come in Stage 3.
- 2026-09-20: Meshing runs on the main thread (64 chunks at boot, then at most 2 dirty chunks per frame); a worker was not needed on desktop and is deferred until device profiling says otherwise.
- 2026-09-20: Chunk meshes are positioned at the chunk origin (vertices are chunk-local) so three.js sorts transparent chunk meshes sensibly.
- 2026-09-20: Water and glass share one transparent Lambert material with depthWrite on; water alpha (0.65) lives in the tile itself.
- 2026-09-20: Shadow camera is snapped to whole blocks around the player so shadow edges do not swim while walking.
- 2026-09-20: Antialias is on (MSAA is cheap on Apple tile-based GPUs); revisit if the iPad profile shows a cost.
- 2026-09-20: Targeting: the white outline follows the screen centre; a tap or long-press acts on the block under the finger (ray from the tap point) within 6 blocks of the eye.
- 2026-09-20: The fence collides as a full cube (kids can step over it) to keep physics simple.
- 2026-09-20: Save format v1: per-chunk diff arrays (Uint16 voxel index + Uint8 block id) plus seed, under idb-keyval key `bv:world`; `bv:meta` holds first-launch time and the storage-persist result.
- 2026-09-20: document-level touchstart/touchmove preventDefault is used to kill Safari double-tap zoom; HUD uses pointer events so it is unaffected. Stage 4 file input must be triggered from pointerup.
- 2026-09-20: Debug overlay text is 16 px (below the 18 px child-facing minimum) because it is a developer tool hidden behind a three-finger tap / F3.
- 2026-09-20: Desktop fallback: F3 toggles the debug overlay, F toggles fly, C toggles camera, Shift sprints (or descends in fly), wheel/1-6 change hotbar slot.
- 2026-09-20: three r186 removed PCFSoftShadowMap; using PCFShadowMap (the current soft PCF filter) instead.
- 2026-09-20: Default third-person pitch is -25° (not -15°) so the screen-centre target lands ~4.5 blocks ahead, inside the 6-block reach.
- 2026-09-20: Touch handler ignores pointerType "mouse" in move/up too: Chrome's mouse uses pointerId 1, which collided with synthetic touch ids during testing; on a real iPad touches and mouse never share ids, but the guard costs nothing.
- 2026-09-20: Lighting balance after screenshots: hemisphere 1.35 / sun 2.0 (was 1.05 / 2.3) so sun-shadowed tree sides do not read as near-black.
- 2026-09-20: Document-level touchmove preventDefault will also block scrolling inside future panels; Stage 3 inventory must opt scrollable elements out (stopPropagation) or use pointer-driven scrolling.
- 2026-09-20: Verified on desktop Chromium (not iPad): 60 fps at DPR 2 with shadows, ~115 draw calls, place/break/particles, autosave round-trip across reload, offline load from the service-worker cache with the server stopped, auto-step, jump height ~1.3, fly, first-person toggle, multi-touch dispatch with synthetic pointer events. Real iPad, Safari, touch hardware and Add-to-Home-Screen are UNTESTED.
- 2026-09-20: A temporary .claude/launch.json was used to run the dev/preview servers during testing and removed afterwards (not part of the project).
- 2026-09-20 (Stage 2): Level formula: total XP to reach level n = round(400 * (n-1)^1.6), which reproduces the two exact numbers in the spec (level 2 = 400, level 3 = 1213); the spec's "≈5,285 / ≈30,000" for levels 5/10 do not fit any reading of the formula, so levels 5 and 10 are 3675 and 13540.
- 2026-09-20 (Stage 2): Ghost voxels are a single translucent GHOST block id (40) in the voxel grid (rendered in the transparent group, non-solid, unbreakable) with a cell map back to the blueprint voxel; they are recorded in the world diff as air so a reload never restores a stray ghost. Filled voxels are ordinary recorded edits.
- 2026-09-20 (Stage 2): Buildings save under `bv:buildings` (instances with a Uint8Array `filled`), profiles under `bv:profiles` ({ current, p1, p2 }); one debounced autosaver handles all keys.
- 2026-09-20 (Stage 2): Blueprints are generated by tools/gen-buildings.mjs into src/data/buildings/*.js (checked in). Counts: Well 14, Hut 38, Fence Garden 30, Farm 68, Tower 80, Stone Hut 110. All fronts face +z (toward the player at spawn); no rotation.
- 2026-09-20 (Stage 2): Placement footprint follows the screen-centre target (green/red box); a tap anywhere confirms, the ✕ in the top bar cancels. Flattening fills below with dirt/grass down to real ground and clears 3 blocks above the blueprint height.
- 2026-09-20 (Stage 2): Ghost cells can be filled from up to 9 blocks away (reach + 3) so tall blueprints are reachable from the ground; normal place/break keep the 6-block reach.
- 2026-09-20 (Stage 2): Breaking a filled block of an unfinished building turns it back into a ghost; on a finished building it opens the ✓/✕ remove dialog. Placing on a ghost fills it regardless of the hotbar selection.
- 2026-09-20 (Stage 2): Awards are written to the profile immediately on completion (safe against force-quit); the HUD counts them in visually (gems fly to the counter ~1.7 s into the celebration; the XP line fills at 2.3 s; a level-up shows its banner after the celebration).
- 2026-09-20 (Stage 2): The level-up banner's "villager walking in" waits for Stage 3 villagers.
- 2026-09-20 (Stage 2): The catalogue card list uses native scrolling with `touch-action: pan-y` and stops touchmove propagation so the document-level scroll blocker does not swallow it (untested on device).
- 2026-09-20 (Stage 2): Pressing Build while positioning a blueprint cancels the placement and opens the catalogue; the progress bar hides during placement so the two top-centre panels never overlap.
- 2026-09-20 (Stage 2): Verified on desktop Chromium (not iPad): new village shows Well + Hut ghosts; Well completed through the real tap path awards exactly 250 XP / 3 gems with orbit, banner, confetti and gem fly-in; catalogue locks (Tower / Stone Hut at LVL 2) and the level pill update; Hut completion levels up to 2 (banner, +5 gems, Tower/Stone Hut unlocked); remove dialog deletes a finished Well; buildings, ghosts, XP and gems survive a reload. Real iPad is UNTESTED for this stage.
- 2026-09-20 (Stage 3): Update toast uses vite-plugin-pwa's `onNeedReload` hook: `registerType` stays `autoUpdate` (the new service worker installs and activates by itself) but the page no longer auto-reloads; the toast "Updated, restart to apply" reloads on tap. The start card also shows the running version (e.g. v0.3.0) so deploys can be confirmed at a glance. Update checks run hourly and whenever the app returns to the foreground.
- 2026-09-20 (Stage 3): Furniture items are voxel blocks with sub-box models (SHAPE.MODEL). Oriented items (bench, bed, shelf, picture, clock) use four block ids (facing 0..3, front +z/+x/-z/-x); the inventory shows facing 0 and placement picks the variant facing the player, or the wall normal for wall items. Doors are two blocks tall (8 ids: axis x/z, open/closed, bottom/top); tapping any half toggles both; breaking either half removes both.
- 2026-09-20 (Stage 3): Animals are entities (not blocks): box models with a painted 32x32 texture per species, wander within 8 blocks of their home, tap to hear a synthesized sound, long-press 0.5 s to remove. Cap 30 animals. Barn completion spawns cow, pig, sheep and chicken.
- 2026-09-20 (Stage 3): Villagers are world state (shared by both kids): one per level gained across all profiles, capped at 12; they reuse the player avatar with 8 skin palettes, wander on a ring around buildings, wave and show an emoji bubble within 3 blocks. Tapping a villager makes it wave.
- 2026-09-20 (Stage 3): World-life state (wheat timestamps, animals, villagers, time of day) saves under `bv:life`; custom blueprints under `bv:custom`. Wheat grows a stage every 3 real minutes from its planting timestamp, so time with the app closed is caught up on load and on foreground.
- 2026-09-20 (Stage 3): Torch/chandelier lights: a fixed pool of 8 PointLights (constant count so the Lambert shader is not recompiled) re-assigned to the 8 nearest sources every 0.25 s, brighter at night. If the iPad profile shows a cost, lower the pool size in main.js.
- 2026-09-20 (Stage 3): Day/night: the sun direction stays fixed (stable shadows); the cycle only dims/recolours the sun, hemisphere, sky and fog through night → sunset → day keyframes over 20 minutes. "Always day" pins mid-day and is the default.
- 2026-09-20 (Stage 3): Swimming: inside water gravity drops to 6, sinking is capped, holding Jump swims up, horizontal speed is 55 %, and a blue tint overlays the screen while the eye is under water. Water renders double-sided so it is visible from below.
- 2026-09-20 (Stage 3): "Save my build" lives in the Build catalogue's second tab (camera icon): two taps pick opposite corners (max 16x16x16, 600 blocks); the blueprint gets an auto name ("My Build n"), 4 XP per block (max 600) and 1 gem per 20 blocks (max 10), and appears with a rendered thumbnail.
- 2026-09-20 (Stage 3): Music (toggle in settings) waits for Stage 4 with the settings menu; ambient birds/crickets are in.
- 2026-09-20 (Stage 3): The inventory sheet sits above the hotbar (hotbar stays visible/selectable); tapping an item fills the selected slot; slots swap by dragging one onto another. The hotbar is saved per profile.
- 2026-09-20 (Stage 3): Gotcha fixed during testing: new save keys must be added to KEYS in save.js; the autosaver now throws on an unknown key instead of failing silently.
- 2026-09-20 (Stage 3): Long-press targets liquids too, so placed water can be scooped back up (taps still pass through water for placement).
- 2026-09-20 (Stage 3): Verified on desktop Chromium (not iPad): every item in the five packs placed and removed through the real tap / long-press paths (44 blocks and items plus 8 animals); door toggles open/closed; wheat advanced 33→34 by ticking and 33→35 across a reload after a 7-minute backdate; torch lights glow at night; 60 fps with 12 villagers and 10 animals in view (about 420 draw calls); minimap, inventory tabs, hotbar swap, Save-my-build with thumbnail, swimming, and the update toast (v0.2.0 page showed the toast after a v0.3.0 build, tapping reloaded into v0.3.0). Real iPad UNTESTED for this stage; draw-call count with all entities in view is the main device risk.
- 2026-09-20 (Stage 4): Profiles: `bv:profiles` holds { current, p1, p2 }; each profile carries xp, gems, unlocked, settings, hotbar, hints and lastChest. Switching swaps skin, level pill, gems, hotbar and settings; the world, buildings, animals and villagers are shared. Villager count = level-ups summed over both profiles.
- 2026-09-20 (Stage 4): Backup file: JSON { format: 'block-village', version, app, exportedAt, data } gzipped with CompressionStream (raw JSON when unavailable), typed arrays tagged as { __ta, d } so chunk diffs round-trip. Shared as `block-village-YYYYMMDD.bv` (application/octet-stream) through navigator.share with files, falling back to an <a download>. Load parses gzip or plain JSON, asks ✓/✕, writes every key and reloads.
- 2026-09-20 (Stage 4): Daily chest is per profile per calendar day (local date string), 5–10 gems, shown right after picking a profile; the day is recorded before the animation so a force-quit cannot farm it.
- 2026-09-20 (Stage 4): Shadows toggle flips the sun's castShadow (three recompiles programs itself); the shadow map stays enabled so no manual material.needsUpdate is needed.
- 2026-09-20 (Stage 4): Music is a synthesized 80 bpm C/Am/F/G arpeggio loop scheduled 0.4 s ahead; off by default, per profile.
- 2026-09-20 (Stage 4): Rotate overlay triggers on height > width from the viewport resize hook (matchMedia orientation is unreliable in standalone mode); the loop skips updates while portrait. Context loss: preventDefault + "One moment…" card; on restore chunk meshes are rebuilt (three re-creates GPU state itself). The debug overlay has a "lose GL" button that forces a loss and restores it 1.5 s later.
- 2026-09-20 (Stage 4): First-time hints are per profile: joystick (done after 1 s of joystick use), look (after 200 px of drag), tap (after the first placed or filled block), build (after opening the catalogue). Only one shows at a time and never over panels.
- 2026-09-20 (Stage 4): The pause menu's file input is triggered from a pointerup (user activation) because touchstart is preventDefault-ed document-wide; untested on iPad until this stage's device pass.
- 2026-09-20 (Stage 4): The rotate overlay is mounted on <body>, not inside #hud: the HUD's bump animation leaves a transform on it, which makes it a stacking context, and the start card (z 10) would otherwise cover the overlay before the first tap.
- 2026-09-20 (Stage 4): Verified on desktop Chromium (not iPad): picker shows Atharv (LVL 2) and Avyaan (LVL 1) with their faces; picking Avyaan swaps to the yellow/green skin, LVL 1, 0 gems and his hotbar while buildings/animals stay; the daily chest gives 5–10 gems once per profile per day; hints run joystick → look → tap → build and are remembered; pause menu with Settings (all four toggles apply and persist) and Backup (Protected flag + version); backup file builds gzipped (1 KB), parses back with every key identical, and Load restores tampered gems; Switch returns to the picker and Atharv comes back at LVL 2 with 22 gems; portrait shows the rotate card and pauses; forced GL context loss shows "One moment…" and the scene rebuilds on restore. The Share sheet, Files round-trip, file picker activation and the 4-year-old test are UNTESTED on device.
